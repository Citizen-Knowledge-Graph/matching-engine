import { describe } from "mocha"

describe.skip("profile manager tests", function () {
    let profileManager

    before(async function () {
        profileManager = globalThis.profileManager
    })

    it("TODO", function () {})
})
