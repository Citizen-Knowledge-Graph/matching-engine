import { describe } from "mocha"

describe.skip("user manager tests", function () {
    let userManager

    before(async function () {
        userManager = globalThis.userManager
    })

    it("TODO", function () {})
})
